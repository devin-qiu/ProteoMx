% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CreateDiscordanceMatrix.R
\name{CreateDiscordanceMatrix}
\alias{CreateDiscordanceMatrix}
\title{Create AOI-normalized Protein Discordance Matrix}
\usage{
CreateDiscordanceMatrix(
  geomx_set,
  neg_probe,
  assay = "q_norm",
  proteins = NULL
)
}
\arguments{
\item{geomx_set}{A \code{NanoStringGeoMxSet} object.}

\item{neg_probe}{Character. The negative-control probe used as the rank reference.}

\item{assay}{Character. Assay to use for expression values. Default is \code{"q_norm"}.}

\item{proteins}{Character vector or NULL. Proteins to include. If NULL, all non-negative
probes except the selected negative probe are used.}
}
\value{
A matrix with proteins as rows and AOIs as columns, containing AOI-normalized
rank-discordance values.
}
\description{
Computes a protein-by-AOI discordance matrix by comparing the AOI rank of each
protein against the AOI rank of a selected negative probe.
}
\details{
For protein i and AOI j:

d_ij = abs(rank_i_j - rank_neg_j)

Then, within each AOI j:

p_ij = d_ij / (sum_i d_ij + 1)

The output value p_ij represents the fraction of AOI-level rank discordance
attributable to protein i, stabilized by adding 1 to the AOI-wise denominator.
}
